# Prompt de Reconstrução — Manus

Construa o aplicativo Expo/React Native **BARRIGAFIT — Desafio de 21 Dias**, mobile portrait e focado em UI/UX. Use TypeScript, Expo Router, AsyncStorage, `expo-linear-gradient`, `expo-notifications`, `expo-haptics`, `expo-keep-awake` e Material Icons. Não use emojis. Utilize `ScreenContainer` em todas as telas e nunca aplique `className` a `Pressable`.

Use o documento `BARRIGAFIT_REBUILD.md` como especificação e os arquivos da pasta `source/` como referência canônica. Preserve os dados de `source/lib/mock-data.ts`, o tema de `source/theme.config.js` e a estrutura de armazenamento de `source/lib/storage.ts`.

Requisitos obrigatórios: login por nome, e-mail e código; perfil editável; painel admin local; criação/pausa de códigos de acesso; lista de usuárias; catálogo local de URLs de vídeo; senha temporária administrativa trocável uma única vez; treino com timer, play/pause e transição 3-2-1; lembrete local diário com permissão; medidas com gráfico e histórico; semana com rótulo dinâmico e domingo correto; barra inferior com cinco abas legíveis; placeholders estáticos em gradiente no lugar de animações SVG.

O e-mail administrativo é `brunobondurant@gmail.com`. A senha temporária é `BF-9X7K-2R4M`. O código inicial de usuária é `BARRIGA21`. Trate essa autenticação como protótipo local. Para produção, implemente backend com hash de senha, sessões seguras, banco de dados e autorização por papel.
